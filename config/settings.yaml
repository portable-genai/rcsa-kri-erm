# RCSA, KRI and ERM Operating Copilot (rcsa-kri-erm) runtime configuration.
#
# Loaded by `src/rcsa_kri_erm/config.py`. Point at a different file with
# ERM_SETTINGS; a path that is named but missing is a boot error, not
# a silent fall-back to built-in defaults.
#
# `${VAR}` and `${VAR:-default}` resolve in THREE states: unset takes the default written here,
# SET-AND-EMPTY REFUSES and the process fails to boot (an operator who emptied a value
# expressed an intent, and it names nothing, so it inherits neither the default written
# here nor the empty string), and a set value wins. This file holds env var NAMES and non-secret defaults
# only: no secret value is ever committed here.

# Data residency. Selected once and shared by the runtime and Terraform.
region: ${GCP_REGION:-asia-southeast1}

# Tenant partition asserted on outbound reviews when the verified principal carries none.
# (Built as one template expression: a literal "${" immediately before a template opener is
# ambiguous to render, so the whole reference is produced by the expression instead.)
tenant: ${ERM_TENANT:-}

# Local profile stores. ":memory:" is the ephemeral, deterministic default the gate uses; a
# deployment points this at a durable path.
audit_path: ${ERM_AUDIT_PATH:-:memory:}

# The external head anchor for the WORM audit chain (practices check C9). The hash chain cannot
# detect a TRUNCATED TAIL by itself: dropping the newest rows leaves a shorter chain that
# verifies perfectly. Point this at a file on a DIFFERENT volume, under different credentials,
# from audit_path, and every append also writes the chain head there. Empty (the default) means
# no anchor, which is correct for the ephemeral ":memory:" store and wrong for a durable one.
audit_anchor_path: ${ERM_AUDIT_ANCHOR:-}

# The audience the managed (gcp) IAP identity adapter verifies the assertion AGAINST: the
# IAP-protected resource, "/projects/<NUM>/global/backendServices/<ID>" behind an HTTPS load
# balancer. It is per-deployment, so it is configuration and never a literal in the adapter.
# Three states. UNSET leaves it empty, and an empty audience REFUSES every caller rather
# than verifying without one; SET-AND-EMPTY is not a way to reach that state, because the
# loader refuses an emptied variable outright. Leave it UNSET to leave it unconfigured.
# google-auth documents audience=None as "the audience is not verified", which accepts ANY
# Google-signed OIDC token, from any project or any app, and reads its email as a principal.
iap_audience: ${ERM_IAP_AUDIENCE:-}

# Rule R8: where an escalated result is routed for human review. The managed profile REFUSES to
# swallow an escalation when this is empty, so a producer cannot ship with R8 unwired.
# The outbound S2S credentials are HUMAN_REVIEW_S2S_TOKEN / HUMAN_REVIEW_S2S_SIGNING_KEY (see .env.secrets.example),
# deliberately distinct from this service's own inbound ERM_S2S_TOKEN.
review_url: ${HUMAN_REVIEW_URL:-}

# The per-port adapter map: the ONLY place a binding lives, so switching the whole stack is a
# configuration change rather than a code edit. Every port must bind every profile; the loader
# refuses a partial block and `tests/test_settings_file.py` fails the build if this drifts from
# `config.DEFAULT_BINDINGS`.
project_id: ${GOOGLE_CLOUD_PROJECT:-}

adapters:
  audit:
    local: rcsa_kri_erm.adapters.local.audit:LocalAuditAdapter
    gcp: rcsa_kri_erm.adapters.gcp.audit:CloudAuditAdapter
    onprem: rcsa_kri_erm.adapters.onprem.audit:OnPremAuditAdapter
  identity:
    local: rcsa_kri_erm.adapters.local.identity:LocalIdentityAdapter
    gcp: rcsa_kri_erm.adapters.gcp.identity:IapIdentityAdapter
    onprem: rcsa_kri_erm.adapters.onprem.identity:OnPremIdentityAdapter
  review_router:
    local: rcsa_kri_erm.adapters.local.review_router:LocalReviewRouter
    gcp: rcsa_kri_erm.adapters.gcp.review_router:CloudReviewRouter
    onprem: rcsa_kri_erm.adapters.onprem.review_router:OnPremReviewRouter

  # Tracing. Which BACKEND a deployed profile exports to is not a binding: the gcp
  # adapter reads OTEL_EXPORTER_OTLP_ENDPOINT and sends OTLP to the agent-observability collector when it
  # is set, straight to Cloud Trace when it is not. Tracing is not essential to
  # correctness, so onprem is ABSENT rather than fatal, unlike the other placeholders.
  tracer:
    local: rcsa_kri_erm.adapters.local.tracer:LocalNoopTracerAdapter
    gcp: rcsa_kri_erm.adapters.gcp.tracer:CloudTracerAdapter
    onprem: rcsa_kri_erm.adapters.onprem.tracer:OnPremTracerAdapter

  # The promotion authority (model-quality-gate). The local adapter scores offline but REFUSES to
  # promote: a promotion certified by a laptop with no quality service is certified by
  # nothing.
  evaluation:
    local: rcsa_kri_erm.adapters.local.evaluation:LocalOfflineEvalAdapter
    gcp: rcsa_kri_erm.adapters.gcp.evaluation:ManagedEvalGateAdapter
    onprem: rcsa_kri_erm.adapters.onprem.evaluation:OnPremEvalAdapter
  generation:
    local: rcsa_kri_erm.adapters.local.generation:LocalGenerationAdapter
    gcp: rcsa_kri_erm.adapters.gcp.generation:CloudGenerationAdapter
    onprem: rcsa_kri_erm.adapters.onprem.generation:OnPremGenerationAdapter
  control_library:
    local: rcsa_kri_erm.adapters.local.control_library:LocalControlLibraryAdapter
    gcp: rcsa_kri_erm.adapters.gcp.control_library:CloudControlLibraryAdapter
    onprem: rcsa_kri_erm.adapters.onprem.control_library:OnPremControlLibraryAdapter
  embeddings:
    local: rcsa_kri_erm.adapters.local.embeddings:LocalEmbeddingsAdapter
    gcp: rcsa_kri_erm.adapters.gcp.embeddings:CloudEmbeddingsAdapter
    onprem: rcsa_kri_erm.adapters.onprem.embeddings:OnPremEmbeddingsAdapter
  metric_feed:
    local: rcsa_kri_erm.adapters.local.metric_feed:LocalMetricFeedAdapter
    gcp: rcsa_kri_erm.adapters.gcp.metric_feed:CloudMetricFeedAdapter
    onprem: rcsa_kri_erm.adapters.onprem.metric_feed:OnPremMetricFeedAdapter
  theme_feed:
    local: rcsa_kri_erm.adapters.local.theme_feed:LocalThemeFeedAdapter
    gcp: rcsa_kri_erm.adapters.gcp.theme_feed:CloudThemeFeedAdapter
    onprem: rcsa_kri_erm.adapters.onprem.theme_feed:OnPremThemeFeedAdapter
